# F-4 セキュリティ更新（2026-07-21）: fastapi は starlette の既知脆弱性
# （PYSEC-2026-248/249 等）を解消できる最小版 0.133.0 に更新。
# 0.133.0 以降は starlette の上限ピンが撤廃され starlette 1.x を許容する。
fastapi==0.139.2
uvicorn[standard]==0.51.0
sqlalchemy==2.0.51
pydantic==2.13.4
# F-4: python-multipart は複数の DoS 系脆弱性を解消した最新版へ更新
python-multipart==0.0.32
reportlab==5.0.0
qrcode[pil]==8.2
psycopg2-binary==2.9.12
google-auth==2.56.0
# F-4: requests は .netrc 資格情報漏洩等（PYSEC-2026-1872/2275）解消版へ更新
requests==2.34.2
# アプリ独自セッション通行証（HS256 JWT）の発行/検証に使用
# F-4: PyJWT の既知脆弱性（PYSEC-2026-175〜179 等）解消版へ更新
PyJWT==2.13.0
# ローカルの backend/.env を読み込む（本番の実 env は上書きしない）
# F-4: python-dotenv の脆弱性（PYSEC-2026-2270）解消版へ更新
python-dotenv==1.2.2
# F-4: fastapi(>=0.133) が許容する starlette の脆弱性解消版を明示ピン
starlette==1.3.1
openai==2.46.0
# openai 1.54.x は httpx に proxies= を渡すため、それを廃止した httpx 0.28 系と非互換。
# 0.27 系に固定して回避する。
httpx==0.28.1
